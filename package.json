{
  "name": "manhwa-reader",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/frontend"
  ],
  "scripts": {
    "setup": "bun install && cargo fetch --manifest-path apps/backend/Cargo.toml",
    "dev": "tauri dev -c apps/backend/tauri.conf.json",
    "build": "tauri build -c apps/backend/tauri.conf.json && mkdir -p dist && cp \"apps/backend/target/release/bundle/dmg/Manhwa Reader\"_*.dmg \"dist/Manhwa Reader.dmg\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
